import 'package:flutter/material.dart';

class Faviorite extends StatelessWidget {
  const Faviorite({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Faviorite"),
    );
  }
}
